{
  "rules": {
    ".read": false,
    ".write": false,

    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": false
      }
    },

    "visits": {
      "$date": {
        ".read": true,
        ".write": false
      }
    },

    "_rateLimit": {
      ".read": false,
      ".write": false
    },

    "_logs": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() == true",
      ".write": false
    },

    "comments": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() == true",

      "$postSlug": {
        ".read": true,

        "$commentId": {
          ".write": "(auth != null && root.child('admins').child(auth.uid).val() == true) || (!data.exists() && newData.hasChildren(['author', 'content', 'createdAt', 'postSlug', 'reportCount', 'lastReportedAt']) && newData.child('author').isString() && newData.child('author').val().length >= 2 && newData.child('author').val().length <= 40 && newData.child('content').isString() && newData.child('content').val().length >= 3 && newData.child('content').val().length <= 500 && newData.child('createdAt').isNumber() && newData.child('createdAt').val() > 1600000000000 && newData.child('createdAt').val() < now + 60000 && newData.child('postSlug').isString() && newData.child('postSlug').val() == $postSlug && newData.child('reportCount').isNumber() && newData.child('reportCount').val() == 0 && newData.child('lastReportedAt').isNumber() && newData.child('lastReportedAt').val() == 0 && !newData.hasChild('isAdmin') && !newData.hasChild('role') && !newData.hasChild('adminNote')) || (data.exists() && newData.child('author').val() == data.child('author').val() && newData.child('content').val() == data.child('content').val() && newData.child('createdAt').val() == data.child('createdAt').val() && newData.child('postSlug').val() == data.child('postSlug').val() && newData.child('reportCount').isNumber() && newData.child('reportCount').val() == data.child('reportCount').val() + 1 && newData.child('reportCount').val() <= 50 && newData.child('lastReportedAt').isNumber() && newData.child('lastReportedAt').val() >= data.child('lastReportedAt').val())"
        }
      }
    }
  }
}
